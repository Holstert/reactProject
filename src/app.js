import React, { Component } from 'react'
import { hot } from 'react-hot-loader'

function view() {
    return (
        <div>
            Hello wor
        </div>
    )
}

export default hot(module)(view)